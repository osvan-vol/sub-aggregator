name: sub-aggregator

runtime:
  image: python:3.11-slim

commands:
  package: ""
  build:
    - "pip install --no-cache-dir --user -r requirements.txt"
  start: "bash start.sh"

port: 5000

repository:
  branch: main
  auto_deploy: true
