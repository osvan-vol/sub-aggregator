name: sub-aggregator

runtime:
  image: python:3.11-slim

commands:
  package: ""
  build:
    - "pip install --no-cache-dir --user -r requirements.txt"
  start: "python -u app.py"

port: 5000

repository:
  branch: main
  auto_deploy: true
